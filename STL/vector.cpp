#include<iostream>
#include<bits/stdc++.h>
using namespace std;
int main()
{
    array<int,5> m={1,2,3,4};
    vector<int> v;
    // // cout<<"the current capacity is of a vector is : "<<v.capacity()<<endl;
    // v.push_back(3);
    // // cout<<"the current capacity is of a vector is : "<<v.capacity()<<endl;
    // v.push_back(1);
    // // cout<<"the current capacity is of a vector is : "<<v.capacity()<<endl;
    // v.push_back(9);
    // // cout<<"the current capacity is of a vector is : "<<v.capacity()<<"an also the size of it is: "<<v.size()<<endl;
    // cout<<"the first element of the vector is: "<<v.front()<<endl;
    // cout<<"the last element of the vector is: "<<v.back()<<endl;
    // v.pop_back();
    // cout<<v.back()<<endl;
    // cout<<v.at(2)<<"is the answer?";
    vector<int> b={1,2,3,4,5};
    for(int i=0;i<5;i++)
    {
        cout<<b[i]<<endl;
    }
    return 0;
}